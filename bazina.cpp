#include <stdio.h>
int main(){
int i,N;
int X,papel,spock,pedra,tesoura,lagarto;
scanf("%d",&N);
for(i=0;i<N;i++)
{
scanf("%d",&X);
if(X=papel pedra || X==papel Spock || X==pedra lagarto || X==pedra tesoura || X==tesoura papel || X==tesoura lagarto || X==lagarto Spock || X==lagarto papel || X==Sponk tesoura || X==Sponk pedra)
{
	printf("Caso #1: Bazinga!");
}
else if(X==X)
{
	printf("Caso #2: Raj trapaceou!");
}
else
{
	printf("Caso #3: De novo!");
}
}
return 0;
}
